# -*- mode: snippet -*-
# name: PyTorch Autograd Function
# key: pytorch:function
# --
class ${1:MyFunction}(torch.autograd.Function):
	"""Some Information about $1"""

	@staticmethod
	def forward(ctx, input):

		return

	@staticmethod
	def backward(ctx, grad_output)

		return
